**Added :**
- `floating-point gain pipeline instead of simple PCM amplification, significantly reducing clipping at high gain`

- `Low-latency DSP processing with minimal added buffering`

- `More optimized resampling with higher-quality interpolation`

- `Reduced memory allocations during the audio callback to minimize stuttering`

- `Consistent gain across sample rates (44.1 kHz, 48 kHz, etc.)`
